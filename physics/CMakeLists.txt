# Physics Libraries  
# These libraries implement domain-specific physics calculations

# Field computation and electromagnetic field classes
add_subdirectory(FieldClasses)

# Signal generation and processing for physics simulations
add_subdirectory(SignalProcessing)

# Antenna models and radiation pattern calculations
add_subdirectory(Antennas)

# Electron trajectory and dynamics simulation
add_subdirectory(ElectronDynamics)

# Waveguide and cavity electromagnetics
add_subdirectory(Waveguides)

# Particle scattering calculations
add_subdirectory(Scattering)